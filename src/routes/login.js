const express = require('express')
const router = express.Router()
const jwt = require('jsonwebtoken')

router.post('/', (req, res) => {
	//will setup token in user cookie
})

module.exports = router

